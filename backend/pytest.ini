[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    database: requires a live PostgreSQL instance (ARGUS_TEST_DATABASE_URL)
    acceptance: end-to-end through the API; needs PostgreSQL, Chroma and the ONNX pack
