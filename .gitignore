# Model weights are fetched, not committed. The InsightFace buffalo_l pack is
# ~197 MB and w600k_r50.onnx alone exceeds GitHub's 100 MB per-file limit.
# See backend/README.md -> "Recognition status" for the expected layout.
models/
*.onnx

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Local configuration and caches
.env
.pytest_cache/
.ruff_cache/
.chroma/
.chroma-bench/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
venv/

datasets/raw/
datasets/processed/LFW_subset/
datasets/processed/LFW_subset_masked/
datasets/processed/LFW_subset_rwmfd/
datasets/processed/RFBL_flat/
datasets/processed/RFBL_flat_masked/
datasets/processed/RFBL_flat/
datasets/processed/RFBL_flat_masked/
datasets/RFBL/
# manifests are small, deterministic, and worth tracking - they're the
# actual provenance record for which images/identities were used.

datasets/masking/masktheface/dlib_models/

embeddings/embeddings.npz
embeddings/chroma/
evaluation/results.json
evaluation/multi_template_results.json

enrollment/enrollment_embeddings.npz
enrollment/chroma_data/
# enrollment_manifest.csv is kept, same policy as the other manifests above

enrollment/rfbl_colab_bundle.zip
enrollment/rfbl_embeddings.npz
enrollment/rfbl_identity_map.csv
# rfbl_manifest.csv is kept, same policy as the other manifests above
